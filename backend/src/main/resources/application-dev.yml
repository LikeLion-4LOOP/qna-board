spring:
  config:
    activate:
      on-profile: dev

  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3307}/${DB_NAME:qna_dev}?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: ${DB_USER:qna}
    password: ${DB_PASSWORD:qna123}

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true

  logging:
    level:
      root: INFO
      com.example.qnaboard: debug
  jwt:
    secret: "CXNS7z5LCOqVE6Y4VuR6IPyru4qvDQv7"
    access-exp-ms: 900000        # 15분
    refresh-exp-ms: 1209600000   # 14일